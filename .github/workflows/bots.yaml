name: Make Site Data on a Schedule

on:
  push:
  schedule:
    - cron: "0 8 * * *"
    - cron: "0 16 * * *"

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      # ...
      - uses: actions/checkout@v3
      - uses: oven-sh/setup-bun@v1

      # run any `bun` or `bunx` command
      - run: bun install
      - run: bun run start
        env: ${{ secrets.DATABASE_URL }}
